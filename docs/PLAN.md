# Lokale-producenten-platform (werktitel "OnlyFarms")

## STATUS (bijgewerkt 3 aug 2026) — wat is er al?

**Gebouwd en live** (onlyfarms-ten.vercel.app):

- **Fase 0+1 volledig**: huisstijl-tokens (terra/ink/cream), i18n-taalbestand, seizoensthema, kaart volledig verwijderd, producers-datamodel (gids/leden-scheiding), catalogus **151 items met elk een eigen SVG** (incl. categorieen Vis en Supermarkt), lijsten met deel-links, matching per item (Haversine, straal + fallback, reistijden), SEO-pagina's (producent/producenten/provincie, sitemap 2.294 URL's, JSON-LD), meldknop, Klaviyo-events (env-guarded).
- **Fase 2 grotendeels**: accounts (registreren, inloggen, wachtwoord wijzigen), **family accounts** met uitnodigingslink `/gezin/[code]`, lijsten claimen, gevalideerde "wie haalt het" (alleen gezinsleden, server-side afgedwongen), waar/wie/wanneer per item met locatietips ("tip van ..."), testaccounts voor Frank/Chimene/Sally (elk eigen gezin).
- **UX-sprint (Bring-niveau)**: optimistic UI (~40 ms reactie), offline-modus (service worker + acties wachten op reconnect), bottom-nav met badge, lijst-switcher, sticky zoeken met categorie-chips, Enter-quick-add, undo-snackbar, hoeveelheid via long-press + presets, wis-gekocht, categorie-groepering (loopvolgorde winkel), onboarding-kaart, PWA-manifest + app-iconen, gerangschikt accent-ongevoelig zoeken met synoniemen, a11y-basis (dialogs, contrast, grote tikvlakken).
- **Data**: 2.279 producenten geseed; ~89% met productdata (afgeleid uit omschrijvingen + steekproef); dubbele adressen gerapporteerd; 3 Belgische records bewust zonder coordinaten.

**Aanvulling 3 aug 2026 (UX-sprint 2 + lokaal-verdieping, live)**: cart-drawer-model (lijst als winkelwagen, Lijst-tab togglet de drawer), zoek-overlay met veld bovenaan (toetsenbord-proof), tik = +1 met teller en vasthouden = aantal/verwijderen, zichtbaar vink-moment, lijsten hernoemen/verwijderen, matching in twee lagen (zeker > categorie-suggestie > supermarkt onderaan) met inline tip per rij, "N in de buurt"-badges op tegels, producenten-zoeken in de zoekbalk, urgentie + eigen categorie-volgorde + vaak-gekocht, 785 producenten verrijkt met specifieke tokens, openingstijden als nu-status (parser voor alle notaties), 275 weekmarkten uit OSM op Ontdek, hier-halen vanaf producentpagina's, Google Places-sync script (wacht op API-key), em-dash-regel doorgevoerd.

**Aanvulling 3 aug 2026, deel 2 (beheeromgeving, live)**: `/beheer` gebouwd, gemerged en op productie geverifieerd met Playwright (26/26 checks: gates, wachtrijen, bewerken, mobiel). Zie § Fase 2.4 voor de inhoud. Inloggen brengt je via `?terug=` automatisch terug naar waar je heen wilde, dus `/beheer` is de enige URL die het team hoeft te onthouden. Accountfilosofie vastgelegd: **één account per persoon, petten bepalen de toegang** (gebruiker, team, straks verkoper via `sellers.user_id`); een boer is immers zelf ook klant. Team en leden krijgen bewust gescheiden omgevingen (/beheer en straks /portaal): harde veiligheidsgrens, per doelgroep één URL; eventueel later één slimme voordeur (/mijn) die op rol doorstuurt. Ter demonstratie van de verkopersflow is "Demoboerderij van Frank" aangemeld en goedgekeurd (seller 4, producent demoboerderij-van-frank, lid, nog zonder adres/producten dus buiten de matching; opruimen = status gestopt).

**Aanvulling 3 aug 2026, deel 3 (producentenportaal v1, live)**: `/portaal` gebouwd en getest (16/16 Playwright-checks). Een aan een gebruikersaccount gekoppelde verkoper ziet er zijn aanmeldstatus en beheert zijn eigen vermelding met een bewuste subset van het beheer-formulier: telefoon, website, omschrijving, openingstijden en product-tokens (naam, adres en status blijven bij het team; opslaan zet "laatst bevestigd"). Koppeling verkoper-account (`sellers.user_id`, uniek): door het team in het beheer (sectie "Portaal-toegang" op de aanmelding) of automatisch bij goedkeuring als het e-mailadres al een account heeft. Zelf claimen met mailverificatie blijft de latere stap (vereist transactionele mail). Ingelogd zonder gekoppelde verkoper krijgt een uitlegpagina met link naar /verkopen; /portaal staat in robots-disallow; profiel toont een portaal-link voor gekoppelde verkopers.

**Aanvulling 3 aug 2026, deel 4 (portaal-profiel met foto's en aanbod, live)**: het portaal is een volwaardig bedrijfsprofiel geworden (11/11 Playwright-checks). Tabs Overzicht/Gegevens/Foto's/Producten. **Foto's** via Vercel Blob (store `onlyfarms-media`, token in Vercel + .env.local): max 8 per vermelding, upload via `/api/upload` (alleen ingelogde verkopers/team, JPG/PNG/WebP tot 8 MB), eerste foto = hoofdfoto; server actions accepteren alleen URL's uit onze eigen store. **Producten** via de bestaande offers-tabel: titel, categorie, prijsindicatie als tekst, foto, beschikbaar-toggle; prikbord-model, expliciete disclaimer "verwerkt geen betalingen" (platformregel). De publieke producentpagina toont nu een fotogalerij en een Aanbod-sectie voor leden. Verwijderde foto's worden ook uit Blob opgeruimd (best-effort).

**Aanvulling 3 aug 2026, deel 5 (UX-sprint "less is more" + swipe, live)**: homepage geminimaliseerd (categorie-grid weg, dat dubbelde de Ontdek-tab; Verkopen-link nu ook op mobiel zichtbaar). **Swipe-tabblad** (4e tab "Swipen", kaartstapel-icoon): Tinder-patroon voor je vaste boodschappen op `/lijst/[token]/swipen`; rechts = toevoegen, links = overslaan, met grote knop-fallback (toegankelijkheid), undo, teller en eindkaart. Deck = eerder gekocht (frequentie) > seizoen > gecureerde `BASICS`; open items worden uitgefilterd. Fundament: **`bought_stats`**-tabel (accumulerende koophistorie per lijst/huishouden, upsert bij afvinken, wisbestendig; de "Eerder gekocht"-suggestierij leest er nu ook uit). Tegelwand op de lijstpagina: **6 tegels per categorie + "Toon alles (n)"** inklapbaar. Labels "Vorige keer gekocht"/"Vaak gekocht" geünificeerd naar "Eerder gekocht". 17/17 Playwright-checks.

**Aanvulling 3 aug 2026, deel 7 (noten en zaden)**: catalogus uitgebreid met walnoot, amandel, cashewnoot, pinda (noten) en zonnebloempit, pompoenpit, lijnzaad, chiazaad, sesamzaad (zaden), elk met een eigen onderscheidend SVG-icoon onder "Overig". Terzijde ontdekt en meteen gefixt: het Nederlandse meervoud "noten" bevat de tekenreeks "noot" niet (klinkerverkorting), waardoor geen enkel notenitem vindbaar was op het meest voor de hand liggende zoekwoord; opgelost met gerichte synoniemen (noot/walnoot/hazelnoot/cashewnoot).

**Aanvulling 3 aug 2026, deel 8 (swipe: winkelmodus vs. smaakmodus bèta)**: het swipe-deck stond de items nog in vaste categorie-volgorde (eerst alle groente, dan fruit), omdat het seizoen/basisset-blok gewoon de catalogus-volgorde volgde. Nu een pil-schakelaar bovenaan het deck met twee modi: **winkelmodus** (standaard) huselt dezelfde kandidatenpool (historie/seizoen/basisset) willekeurig door elkaar, wat aanvullen impulsiever maakt; **smaakmodus (bèta)** weegt diezelfde pool op een geleerd voorkeurssignaal. Elke swipe registreert, in beide modi, een like/skip in de nieuwe **`swipe_signals`**-tabel (los van `bought_stats`: dat is aankoophistorie, dit is voorkeur), zodat de bèta-modus al leert vóórdat iemand er bewust voor kiest. Terugnavigatie vanaf het swipen (header-link, eindkaart, bottom-nav) opent nu altijd de bestaande lijst-drawer (aantallen aanpassen, bewerken, afvinken) in plaats van soms gewoon de winkelpagina.

**Aanvulling 3 aug 2026, deel 9 (persoonlijk smaakprofiel + brede smaakpool)**: smaak is persoonlijk, gezinsleden verschillen. Swipe-signalen horen nu bij de **gebruiker** als je ingelogd bent (over al je lijsten heen), en anoniem bij de lijst (`swipe_signals.user_id`, unique `NULLS NOT DISTINCT` op lijst+item+gebruiker; cascade bij accountverwijdering want het profiel is persoonsdata). De koophistorie (`bought_stats`, winkelmodus) blijft bewust huishouden-breed: wat het gezin koopt is gedeeld, wat jij lekker vindt niet. De smaakmodus-pool is verbreed van historie/seizoen/basisset naar de **hele catalogus** (incl. supermarkt/drogisterij-items, die horen er bewust bij), gewogen op het persoonlijke profiel plus een zetje voor eerder gekocht en seizoen; veel weggeswipete items zakken weg maar verdwijnen nooit helemaal (smaak kan veranderen).

**Aanvulling 3 aug 2026, deel 10 ("je bent vlakbij"-melding v1)**: op de lijstpagina staat onder de locatieregel een instelling "Melding als je vlakbij een verkooppunt bent" (uit/500 m/1 km/2 km, device-lokaal in localStorage, standaard uit). Staat hij aan, dan volgt de pagina je positie zolang hij open is (`watchPosition`) en toont een banner zodra een producent die iets van je open lijst verkoopt binnen de drempel is, met link naar de producentpagina en route; zelfde producent maximaal 1x per 4 uur (sessionStorage). Eerlijke beperkingen: werkt alleen met de app open (echte achtergrond-geofencing kan niet in een PWA, dat wordt de Expo-app) en kandidaten zijn de al gematchte producenten binnen de lijst-radius rond de lijst-locatie.

**Aanvulling 3 aug 2026, deel 11 (screening van verkoper-content)**: nieuwe producten en foto's van verkopers gaan eerst door een teamcontrole voordat ze publiek staan. `offers.published` (bestaand aanbod is bij de migratie op gepubliceerd gezet; nieuw aanbod wacht) en `producers.photos_pending` (uploads komen daar; goedkeuren verplaatst naar de publieke galerij, afwijzen verwijdert ook de blob). Inhoudelijk gewijzigd aanbod (titel/omschrijving/prijs/foto) gaat terug de wachtrij in; alleen de beschikbaar-toggle omzetten niet. Nieuwe beheerwachtrij **`/beheer/aanbod`** met nav-badge en dashboard-tegel. Meteen dichtgezet gat: aanbod van een geschorste verkoper was publiek blijven staan zolang de claim bestond; de publieke query eist nu ook verkoper-status goedgekeurd. Revalidatie van producentpagina's gaat nu via `revalidatePath("/producent/[slug]", "page")` (het oude `"/producent"` raakte de dynamische pagina's niet betrouwbaar).

**Aanvulling 3 aug 2026, deel 12 (verkoper-vraaginzicht)**: het portaal-overzicht toont goedgekeurde verkopers met een vermelding mét coördinaten en producten een sectie "Vraag in jouw buurt": welke catalogusitems staan open op boodschappenlijsten binnen 10 km die de producent (op token-niveau) ook verkoopt. **Geanonimiseerd met drempel**: alleen aantallen en pas tonen vanaf 3 lijsten, zodat nooit één klant herleidbaar is; past bij de platformrol (partijen verbinden, geen transacties). Query in `src/lib/queries/demand.ts` (Haversine over de lists-tabel). Zo leren beide kanten: de koper via smaakmodus en vlakbij-melding, de verkoper via de vraag in zijn buurt.

**Aanvulling 3 aug 2026, deel 13 (Cheffs: de lijst-chat, v1)**: chat per boodschappenlijst tussen **echte mensen** (nadrukkelijk geen AI), zodat je niet naar WhatsApp/iMessage hoeft voor vragen over de boodschappen. Naam van Frank: **"Cheffs"** (bewuste spelfout met dubbel f; enkelvoud cheff, meervoud cheffs). Twee soorten berichten: algemeen ("wat eten we vanavond?", vrij invoerveld) én **gekoppeld aan een item** via het koksmuts-knopje op de itemrij. Item-vragen zijn **bewust alleen presets** (Frank: "alleen maar presets"): aantikken = versturen, geen getyp, grote tikvlakken ("Waarom hebben we dit nodig?", "Weet je het zeker bij deze winkel? Ik ben er nu.", "Zal ik het bij een andere winkel halen?", "Hoeveel heb je er nodig?", "Deze is er niet. Is iets anders ook goed?"). Het bericht draagt een chip "Over: {item}"; het label blijft staan ook als het item later verdwijnt. Schrijven kan alleen met een account (naam uit je account, `list_messages.user_id`); lezen kan iedereen met de lijst-link (zelfde vertrouwensmodel als de lijst zelf, waar gasten ook items mogen bewerken). Ongelezen-teller op de koksmuts-knop in de drawer-header (device-lokaal bijgehouden). Realtime via het bestaande bump/refresh-kanaal. Franks bredere visie ("een dialoog door het gehele systeem"): ankers op winkels/producentpagina's zijn de logische vervolgstap, staat onder Openstaand.

**Aanvulling 3 aug 2026, deel 14 (Cheffs op producentpagina's)**: eerste stap van "dialoog door het hele systeem": op elke producentpagina staat een knop **"Vraag je cheffs"** (alleen zichtbaar met een actieve lijst). Ook hier alleen presets: "Ik ben nu hier. Nog iets nodig?", "Zal ik hier iets van de lijst halen?", "Kennen we deze? Is die goed?". Het bericht belandt in de chat van je actieve lijst met een **klikbare producent-chip** (naam uit de database, nooit client-input; `list_messages.producer_slug/producer_name`). Versturen vereist inloggen (gast krijgt de login-prompt), na versturen een bevestiging met link naar de lijst.

**Aanvulling 3 aug 2026, deel 15 (desktop/responsive-pass)**: Frank merkte op dat de zoekbalk op desktop "naar boven springt": klikken opende de mobiele full-screen-overlay met het veld bovenaan (op mobiel bewust, wegens het toetsenbord; op desktop een glitch). Desktop-audit gedaan (1440x900, screenshots van alle hoofdpagina's) met twee fixes: (1) de zoek-overlay is op desktop nu een **gecentreerd paneel met backdrop** (backdrop-klik sluit; mobiel ongewijzigd full-screen), en (2) de grootste vondst: op desktop was de **lijst-drawer helemaal niet te openen** (de tabbalk is mobiel-only en was de enige toggle) — er staat nu een vaste "Je lijst"-knop met teller rechtsonder. **Responsive-afspraak vastgelegd in AGENTS.md**: mobile-first is leidend (native app komt later), desktop is dezelfde gecentreerde kolom met gecentreerde panelen i.p.v. bottom-sheets, nooit hover-afhankelijk.

**Aanvulling 4 aug 2026, deel 16 (gebruikersprofiel v2)**: /profiel is van een kale pagina een volwaardig accountcentrum geworden. Nieuw: naam wijzigen, huishouden hernoemen/verlaten/uitnodigingscode vernieuwen, account-brede instelling voor de vlakbij-meldingsradius (`users.nearby_radius_m`; wint van de device-lokale waarde zodra je ingelogd bent), **"Jouw smaak"** (transparant inzicht in wat de smaakmodus van je swipes leerde, met wis-knop: controle over je eigen data) plus "vaak gekocht door jullie" (gezinsdata), lijsten met open-teller en laatst-gewijzigd, teamleden krijgen een /beheer-link, en **account verwijderen** (AVG, wachtwoord-bevestiging). Daarvoor zijn vier verwijzingen naar users omgezet naar ON DELETE SET NULL (lijst-eigenaar, item-toewijzing, verkoper-koppeling, melding-afhandelaar): persoonsdata verdwijnt mee, gezinslijsten en koophistorie blijven. Bijvangst: `joinHouseholdAction` gaf "gelukt" terwijl er stil niets gebeurde als je al in een gezin zat; geeft nu een eerlijke foutmelding. Alle profielteksten via t() (was hardcoded). E-mail wijzigen wacht op transactionele mail.

**Aanvulling 4 aug 2026, deel 17 (verkopersportaal v2)**: het portaal-overzicht helpt de verkoper nu actief beter worden. **"Maak je profiel af"**: volledigheids-checklist met percentage; elk ontbrekend punt (foto, omschrijving, uren, telefoon, website, producten, aanbod) is een directe link naar de juiste tab (CRO #44). **"Jouw bereik"**: hoe vaak de zaak gekozen is als ophaalpunt (totaal + 30 dagen, uit `list_items.producer_slug`) en genoemd in de lijst-chat (`list_messages.producer_slug`) — dat "ongebruikte goud" wordt nu ontsloten, geaggregeerd en anoniem. **"Alles klopt nog"**-knop zet de laatst-bevestigd-datum (antwoord op het datakwaliteitsprobleem: 1 record ooit geverifieerd; de publieke pagina toont die datum al). Verkopers beheren nu ook **praktische info** (bio-gecertificeerd, automaat, betaalwijzen — CRO #45; nieuw veldblok in AdminProducerForm, ook voor het team) en hun **contactpersoon-gegevens** (naam/telefoon; e-mail blijft bij het team als koppel-sleutel). Overzichtstegels tonen wachtende foto's/producten mee.

**Aanvulling 4 aug 2026, deel 18 (meetfundament + CRO-borging)**: Frank leverde 100 CRO-hypotheses aan; die staan nu in [docs/CRO-HYPOTHESES.md](CRO-HYPOTHESES.md) met een status-mapping (wat is al gedekt, wat is nu meetbaar, wat komt later). Om de KPI's meetbaar te maken is er een **eigen event-log** (`events`-tabel + `trackConversion()` in `src/lib/events.ts`): privacyvriendelijk (geen extern script, geen cookies), best-effort (meting breekt nooit het kernpad), strikte namen-whitelist. Gelogd worden de primaire conversiemomenten: lijst gestart, product toegevoegd, locatie ingesteld, lokale match bekeken, route geopend (via `/api/event`-beacon), lijst gedeeld (beacon) en producent aangemeld. Het beheer-dashboard toont ze geaggregeerd per 7/30 dagen; terugkeer-binnen-7-dagen is afleidbaar. Klaviyo-events (Sally) blijven ongemoeid.

**Aanvulling 4 aug 2026, deel 19 (CRO-sprint 1)**: de drie meest kansrijke hypotheses uit Franks top-15 gebouwd. **Voorbeeldlijst** (#7): op de homepage "Liever niet typen? Begin met een voorbeeldlijst" — één tik geeft een gevulde lijst met 8 populaire items met brede lokale dekking (`SAMPLE_LIST` in catalog.ts; het lijst-gestart-event draagt `sample: true`, per-item-events worden bewust niet gelogd). **Lokale samenvatting** (#4/#41): boven de tegelwand staat "X van je Y boodschappen zijn lokaal verkrijgbaar", tikbaar naar de lijst-drawer. **"Nu open" prioriteren** (#6/#43): in de match-resultaten en de nabijheids-sheet staan nu-geopende producenten eerst (stabiele hersortering via `openFirst()` in opening-hours.ts, afstandsvolgorde blijft daarbinnen) en krijgt "Nu open, tot X" een terra-chip; gesloten zaken houden de stille grijze "Opent ..."-tekst. Effect is meetbaar via het event-fundament (match_bekeken, route_geopend).

**Aanvulling 4 aug 2026, deel 20 (vorige lijst herhalen, CRO #81)**: in de lijst-drawer staat bij "Onlangs gekocht" nu **"Alles terug op de lijst"**: één tik zet alles wat gekocht was weer open (`restoreBought`, optimistic via de bestaande reducer). Dit is de retentie-hypothese "vorige lijst met één tik herhalen" binnen het bestaande model van blijvende lijsten; de koophistorie (`bought_stats`) blijft intact.

**Aanvulling 4 aug 2026, deel 21 (technisch SEO-fundament)**: volledig en zelfonderhoudend. Centrale laag `src/lib/seo.ts` + `<JsonLd>`: Organization/WebSite (met SearchAction) site-breed, verrijkte LocalBusiness per producent (specifiek type per soort: Brewery/Bakery/Winery/GroceryStore, `@id`, foto's, openingHoursSpecification uit de urenparser, paymentAccepted, OfferCatalog; geo-bug gefixt: alleen complete coördinaten; externe site naar sameAs), BreadcrumbList + zichtbare breadcrumb (levert ook de interne link producent→provincie), ItemList op provinciepagina's. Metadata-hygiëne: metadataBase, og/twitter-defaults, dubbele titel-suffix overal weg, canonicals op alle indexeerbare pagina's, facet-URL's op /producenten noindex+canonical, meta-noindex op lijst/gezin/profiel/lijsten/auth, gestopte producenten noindex. Sitemap: lastModified per producent, provincies uit één gedeelde module (mismatch opgelost), /lijsten eruit. Robots uitgebreid (gezin/profiel/auth/api). /kaart nu een echte 308. Og-images site-breed + per producent (foto-achtergrond met naam-overlay). **`scripts/seo-check.ts`** valideert dit alles (sitemap, canonicals, titels, JSON-LD-structuur, noindex, og-images) en hoort bij elke deploy. Nog voor Frank, ná het definitieve domein: Search Console + Bing (DNS-verificatie), `BRAND.domain` omzetten, deboervinder-301's.

**Aanvulling 4 aug 2026, deel 22 (producentpagina als conversie-landingspagina)**: uitwerking van het teamidee "voor iedere boer een landingspagina die vanuit Google converteert". De 2.279 producentpagina's zijn nu volwaardige landingspagina's: een prominente **hero-CTA** ("Haal je boodschappen bij {naam}" met één knop "Start je lijst met deze N producten") die voor een koude bezoeker ter plekke een lijst aanmaakt, alle producten van de producent erop zet mét de producent als haal-plek, en doorstuurt naar de lijst; bezoekers met een lijst krijgen "Zet deze N producten op je lijst". Plus **lokale interne linking**: onderaan elke producentpagina 6 buren binnen 15 km (SEO-mesh én handig). "Download de app" volgt zodra de native app in de stores staat; het hero-blok is daarvoor de gereserveerde plek (genoteerd in de component).

**Aanvulling 4 aug 2026, deel 23 (lijst-rechten, undo op bulkacties, één "eerder gekocht")**: eerste stap uit een brede audit (producentkant als webshop, lijst-hergebruik/planning, blinde vlekken) die drie scherpe randen dichtzet. **Rechten**: `deleteListAction`/`renameListAction` controleren nu of de aanroeper de lijst mag beheren (`requireListManage` in `src/app/lijst/actions.ts`, nieuw `isHouseholdMember` in accounts.ts); een anonieme lijst (geen eigenaar, geen gezin) blijft, zoals altijd, met alleen de link bestuurbaar, maar een geclaimde gezinslijst kan niet langer door iedereen met een oude deel-link verwijderd of hernoemd worden. **Undo**: "Wis gekochte items" en "Alles terug op de lijst" retourneren nu de geraakte rijen (`clearBought`/`restoreBought` met `.returning()`) zodat de bestaande undo-snackbar ze exact kan terugzetten (`restoreClearedAction`/`recheckItemsAction`, twee nieuwe optimistic reducer-acties); de snackbar zelf werkt nu ook zonder actie (loutere melding, bijv. bij een geweigerde beheeractie). **Eén waarheid**: `boughtBefore` (de "Eerder gekocht"-tegelrij) leest voortaan uit dezelfde huishouden-brede `boughtStatsFor` als het swipe-deck, in plaats van een aparte lijst-scoped aggregatie; een tweede lijst in hetzelfde gezin toont nu meteen wat elders al gekocht is (geverifieerd: lijst B toont "Eieren"/"Verse melk" zonder daar ooit iets te hebben afgevinkt). Bijvangst: `boughtStatsFor`'s optelling over meerdere lijsten werd niet herverdeeld gesorteerd na het samenvoegen; nu wel.

**Aanvulling 4 aug 2026, deel 24 (Cheffs-bugfix + rate limit)**: `messagesForList` haalde de laatste 50 berichten op met `orderBy(asc(createdAt)).limit(50)`, wat feitelijk de **oudste** 50 opleverde; in een lijst met meer dan 50 berichten werden nieuwe berichten dus onzichtbaar. Nu `desc` + `limit` en daarna omgekeerd naar chatvolgorde. Erbij: een rate limit op Cheffs (max 20 berichten per minuut per gebruiker, `countRecentMessagesByUser` in `src/lib/queries/chat.ts`) tegen scriptmisbruik, met een nette melding in zowel de lijst-chat als "Vraag je cheffs" op de producentpagina.

**Aanvulling 4 aug 2026, deel 25 (error/loading-fundament, dode lijst-tokens, sw-privacy)**: er waren nergens `error.tsx`/`global-error.tsx`, dus elke serverfout viel terug op de kale Next-foutpagina, buiten de huisstijl en zonder herstelactie. Nu een `error.tsx` (huisstijl, "Probeer opnieuw" + terug naar home) en een minimale `global-error.tsx` (eigen html/body, voor het zeldzame geval dat de root layout zelf faalt). Een verwijderde of nooit bestaande lijst-token gaf de generieke 404; `src/app/lijst/[token]/not-found.tsx` toont nu "Deze lijst bestaat niet meer" + een knop naar een nieuwe lijst, en ruimt de dode token meteen uit `of_lists` op (anders bleef de Lijst-tab er stilzwijgend naar wijzen). `loading.tsx`-skeletons toegevoegd voor de zwaarste routes (`/lijst/[token]`, `/producenten`, `/producent/[slug]`, `/beheer`). Bijvangst in `public/sw.js`: navigaties naar `/lijst/`, `/profiel`, `/beheer` en `/portaal` werden gewoon in de page-cache gezet, dus op een gedeeld gezinstoestel bleven privépagina's na uitloggen offline opvraagbaar; die vier paden worden nu nooit meer weggeschreven (cache-versie gebumpt om bestaande apparaten meteen schoon te trekken).

**Aanvulling 4 aug 2026, deel 26 (rate limiting + Klaviyo-injectie dicht)**: geen enkel publiek schrijf-endpoint had een limiet, en `reportProducerAction` stuurde het vrij ingetypte, onbevestigde e-mailadres van de melder als Klaviyo-**profiel**-e-mail mee (`trackEvent(..., validEmail)`) — daarmee kon iedereen onder willekeurig andermans e-mailadres een profiel/event in de CRM laten ontstaan. Nieuwe lichte in-memory limiter (`src/lib/rate-limit.ts`, per serverless-instance, geen externe dependency) toegepast op: `/api/event` (120/min/IP), `/api/upload` (30/10min per verkoper/team-account), `/api/sellers/apply` (5/uur/IP), `reportProducerAction` (10/uur/IP, en de melder-e-mail gaat nu alleen nog als property mee, nooit meer als profiel-identiteit), `registerAction` (10/uur/IP) en `createListAction`/`addItemAction` (ruim: 30/min en 120/min per IP, alleen tegen runaway scripts). Bijvangst: ook het inloggen zelf (`authorize` in `src/auth.ts`) kreeg een limiet per e-mailadres én per IP, tegen brute-force (elke poging kost scrypt-CPU, dus dat was ook een DoS-hendel) — geverifieerd dat een normaal, niet-geraakt account gewoon kan inloggen en dat 10+ foute pogingen netjes falen zonder crash.

**Aanvulling 4 aug 2026, deel 27 (laatste scherpe randen: gezin-GET, tikvlakken, i18n-schulden, headers)**: sluit spoor 0 van de audit af. `/gezin/[code]` muteerde op een GET (`await joinHouseholdAction(code)` in de render): een linkpreview of prefetch van een ingelogde gebruiker kon hem ongevraagd van gezin laten wisselen. Nieuw `JoinHouseholdConfirm`-component toont nu eerst "Aansluiten bij {gezin}?" en pas na een tik gebeurt het echt (geverifieerd: GET toont alleen de bevestiging, klikken sluit daadwerkelijk aan, en een gebruiker die al in een ander huishouden zit krijgt de bestaande foutmelding). Tikvlakken vergroot zonder de tegelwand drukker te maken: de drie itemrij-icoonknoppen (koksmuts/potlood/prullenbak) krijgen een onzichtbaar vergroot hitvlak (`-inset-1.5` op een absolute overlay, exact passend binnen de bestaande `gap-3` zodat naastgelegen knoppen niet overlappen), en de kleinere tekstlinks (volgorde aanpassen, omhoog/omlaag, bulkacties, Klaar-knoppen) kregen meer verticale padding. Undo-snackbar heeft nu `role="status"`/`aria-live="polite"`. Hardcoded teksten naar `nl.json` in `InloggenClient.tsx`, `RegistrerenClient.tsx`, `ReportForm.tsx`, `gezin/[code]/page.tsx` en de resterende losse strings in `ListView.tsx` (offline-banner, introkaart, locatiefout, zoek-hint, hoeveelheid-sheet). `.env.example` compleet (`AUTH_SECRET`, `BLOB_READ_WRITE_TOKEN`, `GOOGLE_MAPS_API_KEY`, `DATABASE_URL_UNPOOLED`) zodat een nieuw teamlid de app zonder gokken aan de praat krijgt. Security-headers in `next.config.ts`: `Referrer-Policy: strict-origin-when-cross-origin` (lijst-tokens lekten via de referrer naar externe links), `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff`.

**Aanvulling 4 aug 2026, deel 28 (meetfundament per producent, spoor A start)**: eerste PR van het "boer-als-webshop"-spoor uit de bredere audit. De Route-knop op alle 2.279 producentpagina's (en op elke plek waar een producent in de matching opduikt: nabijheids-sheet, "waar koop je dit"-lijst, vlakbij-melding) had geen enkele meting, en er was geen `producent_bekeken`-event: de meest verkoopbare boer-statistiek bestond simpelweg niet. Nieuw: `CLIENT_EVENTS` in `src/lib/events.ts` als één bron voor de API-whitelist (was eerder los gedupliceerd), `producent_bekeken` toegevoegd aan de conversie-events, `route_geopend` draagt overal een `slug`-property, en `ProducerViewPing` (onzichtbaar client component, 1x per sessie via sessionStorage) logt een bezoek op elke producentpagina. Expression-index `events_slug_idx` op `properties->>'slug'` (via een handgeschreven idempotent script, `scripts/migrate-webshop.ts`: `drizzle-kit push` bleek voor deze wijziging niet veilig scopebaar, zie de nieuwe valkuil in AGENTS.md) maakt "Jouw bereik" in het portaal (volgende PR's) een goedkope query. Zichtbaar op het beheer-dashboard onder Conversiemomenten.

**Aanvulling 4 aug 2026, deel 29 (etalage compleet: de boer als webshop)**: tweede PR van het "boer-als-webshop"-spoor. De producentpagina toonde maar de helft van wat een boer invult: telefoon en betaalwijzen zaten alleen in de onzichtbare structured data, van de 8 toegestane foto's werden er 3 getoond, er was geen deelknop, en een geschorste verkoper hield gewoon zijn "Aangesloten"-badge (`producerBySlug` joint nu op `sellers` via `claimedBySellerId`; de badge kijkt naar `sellerStatus`, de claim-teaser bleek al goed gescheiden omdat die op `isMember` filtert). Nu zichtbaar: `tel:`-knop, betaalwijzen en soort bedrijf (nieuwe `producers.kind.*`-labels) in het infoblok, een NIX18-melding bij alcohol-producten of brouwerij/wijngaard, een horizontale foto-scrollrij met alle foto's, en een deelknop (`ProducerShareButton`, OS-share met kopieer-terugval). Aanbod is nu een echte etalage: gegroepeerd per categorie (CATEGORIES-volgorde), een nieuw `offers.featured`-veld licht een product uit (staat bovenaan binnen zijn categorie, telt niet als inhoudelijke wijziging voor de screening, net als de beschikbaar-toggle), en niet-beschikbaar aanbod verdwijnt niet meer stilletjes maar staat gedempt onder "Nu even niet". Geverifieerd met een testproducent (8 foto's, telefoon, betaalwijzen, bier-token, 4 aanbod-items in 2 categorieën) en een geschorste-verkoper-scenario: 17/17 Playwright-checks, seo-check groen.

**Aanvulling 4 aug 2026, deel 30 (ervaringen: alleen tekst, sanering rating/API)**: derde PR van het "boer-als-webshop"-spoor, voert het bestaande PLAN-besluit "`farm_reviews` vervalt → `experiences`, alleen tekst, geen rating" eindelijk door. De `seller_reviews`-tabel had nog een `rating`-kolom en er bestond geen publiek formulier of publieke weergave: de moderatiewachtrij en de tabel bestonden, maar niemand kon er iets in krijgen behalve via een dode, kapotte API. Schema (destructief, eerst leegte geverifieerd): rijen zonder tekst verwijderd, `rating`-kolom weg, `comment` NOT NULL. Nieuw `src/lib/queries/experiences.ts` (`publishedExperiencesForSeller`, `createExperience`) en `submitExperienceAction` (honeypot, rate limit 5/uur/IP, alleen bij een goedgekeurde gekoppelde verkoper) voeden een ingeklapt formulier (`ExperienceForm`) en een "Ervaringen"-sectie op de producentpagina (naam + maand/jaar, geen sterren). Sanering: `/api/sellers` (dode dump die op gemiddelde rating sorteerde, gebruikte de nu-verwijderde kolom) en `/api/sellers/[id]/reviews` (had een bug waarbij één review per e-mailadres de vorige stil overschreef, iedereen met een bekend e-mailadres kon zo andermans ervaring wissen) zijn verwijderd; het publieke formulier vervangt ze volledig zonder de overschrijf-bug (dubbele inzendingen komen gewoon allebei in de wachtrij). `publishReviewAction` stuurt nu ook een Klaviyo `experience_published`-event (sellerEmail via join) en revalidatet de producentpagina. Geverifieerd end-to-end: anonieme inzending → wachtrij (geen rating zichtbaar) → publiceren → publiek zichtbaar, dode routes geven 404, seo-check blijft zonder `aggregateRating` (bewust: reviews zijn niet publiek-vergelijkbaar zolang er geen cijfer aan hangt, Google-eis).

**Aanvulling 4 aug 2026, deel 31 (portaal zonder stille fouten: chip-picker, uren-echo, vakantie)**: vierde PR van het "boer-als-webshop"-spoor. Het portaal-formulier was het teamformulier met wat velden weggehaald, met twee stille faalpaden voor een verkoper die de matching-taal niet kent. **Producten**: het productenveld accepteerde vrij getypte tokens zonder enige validatie, dus een typefout (of een verzonnen woord) werd stil opgeslagen en matchte vervolgens nergens meer, zonder dat de verkoper dat ooit zou merken. Nieuw `KNOWN_TOKENS`/`tokensByCategory()` in `catalog.ts` (bron: de bestaande `matchTokens` uit de catalogus, per token toegewezen aan de categorie van zijn "eigen" catalogusitem); het portaal krijgt nu een chip-picker per categorie (`AdminProducerForm`'s nieuwe `productsPicker`-modus, grote tikvlakken, alleen-aantikken) in plaats van vrije invoer, en `updateOwnProducerAction` valideert server-side alsnog tegen `KNOWN_TOKENS` als defense-in-depth. Beheer houdt zijn eigen vrije-invoer-met-autocomplete (het team kent de tokens toch al). **Openingstijden**: een verkoper kreeg geen enkele feedback of zijn ingevoerde notatie ook daadwerkelijk begrepen werd. Nieuw `describeHours()` in `opening-hours.ts` (puur, hergebruikt de bestaande parser) toont live onder het veld "Dit lezen we als: di & za 08:00-17:00", of een vriendelijke waarschuwing met voorbeeldnotatie als niets herkend wordt. **Vakantie**: nieuwe `producers.closed_until`-kolom (verloopt vanzelf, geen cron, matching blijft ongemoeid) met een `VacationToggle` in het portaal (`setClosedUntilAction`, max 1 jaar vooruit); de publieke pagina toont dan "Tijdelijk gesloten tot {datum}" in plaats van de normale open-status. **Bewust niet gedaan**: het vierde punt uit deze PR, een echt contactkanaal onder de portaal-statusteksten ("Neem contact met ons op" wijst nergens naartoe), is overgeslagen. Er staat geen enkel support-e-mailadres in de codebase en dat verzin je niet; zie Openstaand hieronder. Geverifieerd: chips togglen en verschijnen direct op de publieke pagina en in de "zet op je lijst"-chips, foute urennotatie waarschuwt terwijl geldige notatie echoot, vakantiestatus zichtbaar en weer uit te zetten, `drizzle-kit push` bevestigt de nieuwe kolom zonder verder ruis.

**Aanvulling 4 aug 2026, deel 32 (trots en bereik: bereik-tegels, QR/A5-poster, Klaviyo-signalen)**: vijfde PR van het "boer-als-webshop"-spoor, geeft een boer iets om trots op te zijn en tastbaar te promoten. `producerEngagement()` breidt uit met `views30d`/`routeClicks30d` (uit de `events`-tabel via de `events_slug_idx` uit het meetfundament: goedkoop, geen nieuwe tabel) en er is een nieuwe `topItemsForProducer()` ("meest op lijstjes gezet bij jou", zelfde anonimiteitsdrempel van 3 lijsten als "Vraag in jouw buurt"). Het portaal-overzicht toont nu vier bereik-tegels (bekeken 30d, route geopend 30d, gekozen als ophaalpunt, genoemd in de lijst-chat) plus die top-items-lijst. Nieuw promotiemateriaal: `src/lib/qr.ts` (dependency `uqr`, MIT/zero-deps, `renderSVG`) genereert een SVG-QR in huisstijlkleuren (ink-900/cream-50, gespiegeld uit globals.css: een losse SVG-string leest geen CSS-variabelen, zelfde aanpak als de og-images); `PortalPromote`-blok op het overzicht (publieke URL, deelknop, QR-preview) en een nieuwe printbare `/portaal/promotie`-pagina (A5-formaat via een pagina-eigen `@page`-regel, grote QR, "Zet {naam} op je boodschappenlijst", printknop die met `print:hidden` en de portaal-header zelf niet meeprint). Tot slot twee Klaviyo-signalen die het gemis aan transactionele mail voor een deel opvangen: `publishOfferAction` stuurt nu `offer_published` en `approvePhotoAction` stuurt `photo_approved` (sellerEmail via een kleine uitbreiding op `publishOffer`/`approveProducerPhoto` in `admin.ts`, die nu ook de verkoperdata teruggeven). Best-effort zoals alle Klaviyo-events hier: geen bezorgingsgarantie, Sally bouwt de flows. Geverifieerd: bereik-tegels en QR zichtbaar en scanbaar op het overzicht, poster-pagina toont noindex + juiste inhoud zonder portaal-chrome bij printen.

**Aanvulling 4 aug 2026, deel 33 (/verkopen als warme funnel + sanering)**: zesde en laatste PR van het "boer-als-webshop"-spoor. De claim-teaser op een gidspagina linkte naar een kaal `/verkopen` zonder enige context, terwijl het team achteraf moest gokken tussen 2.279 gids-records welke aanmelding bij welke vermelding hoorde. De claim-teaser linkt nu naar `/verkopen?vermelding=<slug>`; de pagina (nu een server component) haalt die producent op en geeft 'm als `prefill` door aan `VerkopenClient`, die een chip "Je meldt je aan voor {naam} in {plaats}" toont en naam/plaats vooraf invult. Nieuwe `sellers.claim_producer_slug`-kolom bewaart de slug (met een bestaat-en-nog-niet-lid-check server-side, want een POST-body is door de bezoeker te manipuleren); `/beheer/aanmeldingen/[id]` toont bij een gevulde claim-slug een uitgelichte voorstelkaart met het gids-record al aangevinkt, zodat één tik op Goedkeuren volstaat in plaats van eerst te moeten zoeken. Klaviyo `seller_applied` (met e-mailadres) geeft Sally een aanknopingspunt voor een bevestigingsflow zonder dat daar transactionele mail voor nodig is. **Sanering van `VerkopenClient.tsx`**: alle hardcoded Nederlandse tekst (ook de twee al bestaande maar ongebruikte `sell.*`-vertaalsleutels) is nu via `t()` verplaatst naar `nl.json`, "OnlyFarms" komt alleen nog via `BRAND.name` binnen, en de foutmeldingen gebruiken nu `terra`-tokens in plaats van een losse `red-50`/`red-700` (die kleurfamilie stond buiten de toegestane terra/ink/cream-set). **Bijvangst tijdens het testen**: een dubbele KVK/e-mail-aanmelding gaf altijd de kale "aanmelden mislukt" in plaats van de bedoelde "dit KVK-nummer of e-mailadres is al aangemeld" — Drizzle wrapt de echte postgres-foutmelding in `err.cause`, en de bestaande check las `err.message` (alleen "Failed query: ..."). Dat las nooit "duplicate key", dus de vriendelijke 409-tekst werd nooit getoond. Nu gefixt door eerst `err.cause` te lezen. Geverifieerd: claim-link bevat de juiste slug, prefill-chip en vooringevulde velden kloppen, aanmelding met een geclaimde slug komt met de juiste `claim_producer_slug` in de database terecht, de voorstelkaart in het beheer koppelt in één tik, en een bewust dubbele KVK geeft nu de juiste vriendelijke foutmelding.

**Aanvulling 4 aug 2026, deel 34 (lijstenbeheer volwaardig: pin, dupliceren, switcher met gezinslijsten)**: eerste PR van het "lijst opslaan/hergebruiken/plannen"-spoor, bouwt op de rechtencheck uit PR 0.1. `/lijsten` was kaal (alleen naam + link) terwijl de query voor tellers en datums (`listsWithCounts`) al bestond voor het profiel; de pagina gebruikt 'm nu ook, met per rij hernoemen (inline), verwijderen (confirm-tap, zelfde patroon als de lijstpagina), dupliceren en een pin-toggle. Nieuw `duplicateList()` in `lists.ts`: nieuwe token, locatie/straal/categorievolgorde mee, alleen open items (geen gekochte items, geen toewijzing/einddatum: een echt nieuw exemplaar) + `duplicateListAction` (niet-destructief, dus geen beheerrecht nodig net als lezen/toevoegen; claimt de kopie voor een ingelogde gebruiker, `lijst_gestart`-event met `via: "duplicaat"`). **Vaste lijst**: nieuwe `of_pinned`-sleutel naast `of_lists`, een lijst pinnen wint voortaan van "meest recent bezocht" voor alle apparaat-navigatie (tabbalk-knop, swipe-redirect, "zet op je lijst" vanaf een producentpagina, Cheffs vanaf een producentpagina) — precies het `activeToken()`/`activeList()`-koppel uit het nieuwe `src/lib/lists-local.ts`. Dat bestand centraliseert ook meteen de opslag/lees-logica die zevenvoudig gedupliceerd was (elk component z'n eigen `subscribeStorage`+`JSON.parse`+`lists[0]`-aanname, gevonden in `AppShell`, `SwipeRedirect`, `ProducerActions`, `ProducerHeroCta`, `AskChefsButton`, `HomeListPanel`, `ListsClient`, `ListView`, `DeadListCleanup`), inclusief een eigen `of:lists-changed`-event zodat schrijven en lezen binnen hetzelfde tabblad elkaar ook zien (het native `storage`-event vuurt alleen cross-tab; bijvangst: als je gepinde lijst verwijderd wordt, ruimt `forgetList` de pin nu ook meteen op, anders bleef `of_pinned` naar een dode token wijzen). **Switcher**: `ListView` krijgt een nieuwe `serverLists`-prop (uit `listsForUser` op `lijst/[token]/page.tsx`) die met het apparaat-geheugen samengevoegd wordt, zodat een gezinslijst die je op dit apparaat nog nooit bezocht hebt toch in de switcher staat; cap omhoog van 6 naar 10 met een scrollbare sectie, plus een "Maak dit je vaste lijst"-knop en een "Vast"-badge. Twee nieuwe iconen in huisstijl (`CopyIcon`, `PinIcon`, lijnstijl zoals de rest van `icons.tsx`). Bijvangst: twee hardcoded strings op de producentpagina ("Zet op je lijst", "Bekijk je lijst") naar `t()`. Geverifieerd: lijst A pinnen, lijst B bezoeken, tabbalk opent A; `/lijsten` toont tellers/datums/badge; hernoemen/verwijderen/dupliceren werken inline; dupliceren kopieert aantoonbaar geen gekochte items (apart geverifieerd met een open + een gekocht testitem); pin-afzetten werkt.

**Aanvulling 4 aug 2026, deel 35 (vaste boodschappen + lijst_herhaald-event)**: tweede PR van het "lijst opslaan/hergebruiken/plannen"-spoor. De data voor "jouw vaste boodschappen" lag er al (`bought_stats`, huishouden-breed, wisbestendig) maar niets deed er iets mee. Model-afweging (bewust géén aparte sjablonen-tabel of wekelijks dupliceren, zie PLAN.md): "vaste boodschappen" is gewoon een frequentie-drempel op de bestaande koophistorie. Nieuw `frequentBought(list, minTimes=3, limit=15)` in `lists.ts` (zelfde scope-predicaat als `boughtStatsFor`) en `addStaplesAction`: voegt alles toe dat minstens 3x gekocht is en nog niet open staat, heropent afgevinkte items idempotent (hergebruikt `addItem`s bestaande reopen-logica, telt dus niet dubbel in `bought_stats`). Nieuwe knop "Zet mijn vaste boodschappen erop" op twee plekken (leeg-lijst-staat en boven de "Eerder gekocht"-tegelrij), alleen zichtbaar als er staples zijn die nog niet open staan; undo gaat op `catalogKey` in plaats van id (nieuwe reducer-actie `removeByCatalogKeys`), omdat de net toegevoegde items nog een tijdelijk temp-id hebben zolang de server-refresh niet is aangekomen. Nieuw event `lijst_herhaald` (whitelist in `events.ts` + dashboardlabel) met een `via`-property (`"terugzetten"` voor de bestaande "Alles terug op de lijst"-knop, die dit event nog niet stuurde, en `"vaste-boodschappen"` voor de nieuwe knop) en een `n`-property (aantal). Geverifieerd: koophistorie opgebouwd (3x hetzelfde item afgevinkt via de idempotente heropen-cyclus), knop verschijnt op beide plekken, tik voegt toe met werkende undo, `lijst_herhaald` verschijnt op het beheer-dashboard.

**Aanvulling 4 aug 2026, deel 36 (vaste boodschappendag, in-app)**: derde PR van het "lijst opslaan/hergebruiken/plannen"-spoor, laag 1 van CRO #83 (zonder infra: geen mail, geen cron, nog gewoon binnen de app). Nieuwe `users.shopping_day`-kolom (0=zondag..6=zaterdag, null=uit) met `updateShoppingDay()`/`setShoppingDayAction` en een `ShoppingDaySetting` in het bestaande Instellingen-blok op `/profiel`: zeven dag-chips + "uit", zelfde grote-tikvlakken-stijl als de rest van de chip-pickers dit spoor. `DAY_NAMES`/`DAY_SHORT`/`nowInAmsterdam()` in `opening-hours.ts` zijn nu geëxporteerd (was intern) zodat er één bron blijft voor de dag-nummering; nieuwe `todayInAmsterdam()` levert de kalenderdatum voor de dismiss-sleutel. Op de lijstpagina verschijnt op de boodschappendag zelf een prompt-tegel ("Vandaag is je boodschappendag") bovenaan, maar alleen als er ook echt iets te herhalen valt (gekochte items óf ongebruikte vaste boodschappen) — anders is het ruis. De twee knoppen in de prompt hergebruiken gewoon de bestaande `restoreAllBought`/`addStaples`-functies uit de vorige twee PR's (geen nieuwe logica, alleen een nieuwe ingang); dismiss is device-lokaal per lijst per dag (`of_dayprompt:<token>:<datum>`) en overleeft een reload maar niet de volgende dag. Uitgelogd of zonder ingestelde dag: nooit een prompt (shoppingDay komt uit het account, niet uit de lijst). Geverifieerd: geen boodschappendag ingesteld → geen prompt; wel ingesteld maar niets te herhalen → geen prompt; dag + iets te herhalen → prompt met werkende knoppen; dismiss overleeft een reload; uitgelogd bezoek aan dezelfde lijst toont nooit een prompt (bevestigd via een losse, niet-ingelogde sessie).

**Aanvulling 4 aug 2026, deel 37 (plannen: dueAt activeren)**: vierde PR van het "lijst opslaan/hergebruiken/plannen"-spoor. Het `dueAt`-veld bestond al in het datamodel maar was een dood veld: een kaal date-input zonder enige groepering of overzicht. Bewuste afweging (zie PLAN.md): geen aparte `/agenda`-route of maandkalender, de drawer blijft het enige lijst-oppervlak; een compacte week-strip geeft het agenda-gevoel binnen hetzelfde scherm. In `ItemEditor` is het "Uiterlijk"-veld nu een chip-rij: Vandaag, Morgen, de korte naam van je vaste boodschappendag (fallback za), "Kies datum" (pas dan verschijnt het date-input) en "Geen". De drawer krijgt een segmented toggle "Categorie | Dag" naast "Artikelen (n)" (voorkeur device-lokaal per lijst, key `of_viewmode:<token>`); in dag-modus groeperen dezelfde open items zich in emmers (Vandaag inclusief te laat, Morgen, Deze week, Later, Zonder datum) via nieuwe `bucketKeyForDate()`, met een 7-daagse week-strip erboven (dag + open-teller) die naar de bijbehorende emmer-anchor springt (zelfde `#anchor`-patroon als de bestaande categorie-springer op de tegelwand). Tijdzone-risico bewust vermeden: alle vergelijkingen op "YYYY-MM-DD"-datumstrings (nieuwe `addDays()`/`weekdayOf()`/`nextDateForWeekday()`-helpers), nooit op epoch-milliseconden. Een nieuwe optimistic reducer-actie `setDue` zet een item bij het opslaan van een datum direct in de juiste emmer, met een scroll-naar-emmer erachteraan in dag-modus. Geen schema- of actionwijziging nodig: `updateItemAction`/`dueAt` bestonden al. Geverifieerd: chips zetten de juiste datums, dag-modus groepeert meteen correct in drie emmers (Vandaag/Later/Geen) met kloppende koppen, categorie-modus blijft regressievrij (volgorde-knop, indeling), en de toggle-keuze overleeft een reload.

**Aanvulling 4 aug 2026, deel 38 (marktdag-koppeling op de lijstpagina)**: vijfde PR van het "lijst opslaan/hergebruiken/plannen"-spoor. `nearbyMarkets()` (weekmarkten uit OSM) bestond al en werd gebruikt op `/producenten`, maar nooit op de lijstpagina zelf, waar de koppeling met "wat moet ik halen" het nuttigst is. Nieuwe `nextOpenDay()` in `opening-hours.ts` (hergebruikt `parseHours`/`nowInAmsterdam`) geeft de eerstvolgende dag met een interval terug ("vandaag"/"morgen"/dagnaam); de lijstpagina zoekt de dichtstbijzijnde markt binnen 2 dagen (van de 3 dichtstbijzijnde binnen 15 km, `nearbyMarkets`' eigen straal) en toont dan een banner-tegel onder de locatieregel: "{dag} markt in {stad} ({km} km): {n} boodschappen van je lijst vind je daar vaak" + een routelink (met `route_geopend`-beacon, geen slug want geen producent) + een `Bron: OpenStreetMap`-vermelding (ODbL-attributie, apart van de bestaande vermelding op `/producenten` want dat is een andere weergave van dezelfde data). De teller telt bewust open items met niet-lege `matchTokens` (dezelfde "dit soort product is lokaal te vinden"-classificatie als de catalogus, niet een harde producent-match): een markt heeft geen vast assortiment, dus de tekst belooft expres niets concreets ("vind je daar vaak"). Dismiss is device-lokaal per markt per eerstvolgende-datum (`of_markettip:<token>:<marktId>:<datum>`), waardoor hij vanzelf terugkomt zodra er een nieuwe eerstvolgende marktdag is. Onparseerbare `daysText` levert stil geen tip op (geen worst-case aanname). Geverifieerd met een testmarkt (dagelijks geopend, dus altijd "vandaag") en een testlijst op dezelfde coördinaten: tegel toont de juiste tekst met teller en bronvermelding, routelink werkt, dismiss overleeft een reload.

**Aanvulling 4 aug 2026, deel 39 (Vercel Cron + Klaviyo boodschappendag-event, opt-in)**: zesde en laatste PR van het "lijst opslaan/hergebruiken/plannen"-spoor, en daarmee het einde van de volledige 17-PR-audit (spoor 0, boer-als-webshop, lijst-hergebruik/plannen). Laag 2 van CRO #83: de eerste herinnering die buiten de app komt. Nieuwe `users.reminder_opt_in`-kolom (`false` by default, AVG: mail alleen na expliciete keuze) met een checkbox "Mail me ook op mijn boodschappendag" onder `ShoppingDaySetting` op `/profiel`, alleen zichtbaar met een ingestelde dag; de dag uitzetten zet de opt-in ook automatisch uit (anders blijft er een toestemming hangen voor een dag die niet meer bestaat). Nieuw `vercel.json` met één cron (`0 5 * * *` → `/api/cron/boodschappendag`, binnen de Hobby-limiet van 2 crons/1x per dag) en de route zelf: een `Bearer $CRON_SECRET`-guard (401 zonder of met verkeerde header; Vercel stuurt die header automatisch mee op geplande runs, en het project bleek al een `CRON_SECRET` in `.env.local` te hebben staan zonder dat die ooit naar Vercel gepusht was — nu ook in Production gezet), bepaalt zelf de weekdag (Europe/Amsterdam) en stuurt per opt-in-gebruiker met die boodschappendag een Klaviyo-event `boodschappendag` (listName, listUrl met `#lijst`, openItems, staples) naar hun e-mailadres: het eerste niet-anonieme event in de hele app, waar Sally een echte flow op kan bouwen. Best-effort per gebruiker (één mislukte gebruiker breekt de rest niet) en een `?dry=1`-modus die alles behalve de daadwerkelijke Klaviyo-call uitvoert, voor veilig testen. Web push (laag 3) bewust niet nu: iOS heeft de PWA geïnstalleerd nodig, en de Expo-app levert dat later robuuster. Geverifieerd: geen/verkeerde header geeft 401, dry-run met een testaccount (boodschappendag = vandaag, opt-in aan) telt de juiste gebruiker en lijst, een echte run (lokaal ongevaarlijk: geen `KLAVIYO_PRIVATE_KEY` ingesteld, dus geen enkele verzending mogelijk) idem, en de profiel-checkbox toont/verbergt zich correct en is uit te zetten.

**Aanvulling 5 aug 2026, deel 40 (registratie-event + via-conventie op lijst_gestart)**: eerste PR van "Batch 18" (bewust uitgestelde audit-features + SEO/groei-ideeën), klein en mechanisch vooraf gepland zodat spoor 2 er verderop op kan aansluiten. Registreren stuurde alleen een Klaviyo-event (`account_created`, alleen zichtbaar voor Sally); nieuw event `account_aangemaakt` in de `CONVERSION_EVENTS`-whitelist zodat het ook op het beheer-dashboard telt, met `viaInvite` als property (eigen huishouden versus aansluiten via een uitnodigingscode). Daarnaast kreeg `createListAction` een `via`-parameter (default `"lijst"`) zodat de drie bestaande `lijst_gestart`-aanroepen (gewone start, voorbeeldlijst, dupliceren) en de vier plekken die een lijst impliciet aanmaken (`ProducerActions`/`ProducerHeroCta` bij toevoegen vanaf een producentpagina zonder actieve lijst: `via="producent"`; `ListsClient` op `/lijsten`: `via="lijsten"`; `HomeListPanel` op de homepage: `via="homepage"`) allemaal een betekenisvolle, onderscheidende waarde meesturen in plaats van drie keer dezelfde generieke naam. Geen schema-wijziging (properties is al `jsonb`). Geverifieerd tegen een lokale productie-build: een nieuwe registratie zet `account_aangemaakt` met `viaInvite:false` in de database, een lijst starten vanaf de homepage zet `lijst_gestart` met `via:"homepage"`, en het beheer-dashboard toont het nieuwe event onder het label "Account aangemaakt".

**Aanvulling 5 aug 2026, deel 41 (aanbod laten verjaren: "Klopt nog" per product)**: tweede PR van "Batch 18", eerste van de bewust uitgestelde audit-features. `updateOffer()` bumpt bij een inhoudelijke wijziging altijd `updatedAt` en zet zelfs `published:false`, wat niet hetzelfde is als "een verkoper heeft dit recent bevestigd nog te kloppen" — er was geen enkel signaal of een aanbod-item nog actueel is. Nieuwe `offers.last_verified_at`-kolom (nullable timestamptz), nooit door `updateOffer()` zelf gezet: alleen de nieuwe `confirmOffer()`/`confirmOfferAction()` zet 'm, exact zoals `confirmListingAction()` voor de hele vermelding al deed. Nieuwe `ConfirmOfferButton` (`PortalExtras.tsx`) op elke productrij in `/portaal/producten`, maar alleen zichtbaar als een product nooit of langer dan 90 dagen geleden bevestigd is (drempel in JS op de al-opgehaalde rij, zelfde soort staleness-check als `scripts/google-sync.ts`). **Bug gevonden tijdens het eigen testen**: de eerste versie checkte de 90-dagen-staleness vóór de net-bevestigd-state, waardoor `router.refresh()` na een succesvolle bevestiging de knop meteen liet verdwijnen (de nieuw opgehaalde, niet meer stale `lastVerifiedAt`-prop won meteen van de lokale `done`-state) — de gebruiker zag dus nooit de bevestiging "Bedankt, vastgelegd", alleen een knop die spoorloos verdween. Volgorde omgedraaid (`done` eerst, dan pas de staleness-check) zodat de bevestiging altijd zichtbaar is na een klik, en de knop pas bij een latere paginaload weer verdwijnt. Geverifieerd tegen een lokale productie-build met een test-aanbod (`last_verified_at` eerst op `null` gezet): knop zichtbaar bij een nooit-bevestigd product, bevestigingstekst verschijnt direct na de klik, en na een volledige reload is de knop weer weg (recent bevestigd, binnen de 90 dagen).

**Aanvulling 5 aug 2026, deel 42 (aanbod koppelen aan de matching-catalogus)**: derde PR van "Batch 18". Een los aanbod-item had alleen de brede 13-categorie-dropdown (`category`), geen link naar de fijnmazige matching-catalogus (~138 items) waar de rest van de app op draait. Nieuwe `offers.catalog_key`-kolom (nullable text, app-side gevalideerd via `catalogItem()`, nooit FK-afgedwongen: zelfde patroon als `list_items.catalogKey`). Nieuwe `catalogByCategory()` in `catalog.ts` spiegelt `tokensByCategory()` maar op item-niveau (gefilterd op `matchTokens.length > 0`, anders zou een verkoper een item kunnen kiezen dat toch nergens matcht); het portaal-productformulier (`OfferForm.tsx`) krijgt een `<select>` met `<optgroup>` per categorie, naast (niet in plaats van) de bestaande brede categorie. Op de publieke producentpagina toont een gekoppeld aanbod-item nu een klein "Past bij: {label}"-regeltje. Bewust géén unpublish-trigger: catalogKey staat niet in `saveOfferAction`'s `contentChanged`-lijst, want het is een gevalideerde keuze uit een vaste lijst (geen vrije tekst die herscreend moet worden), zelfde redenering als de bestaande `available`/`featured`-toggles. **Bewust niet in deze PR**: catalogKey meenemen in de lijst-matching zelf (`ItemMatch`/`lijst/[token]/page.tsx`) — vereist een nieuwe join over offers→sellers→producers binnen een straal en is een aparte, grotere klus zodra er gebruiksdata is. Geverifieerd tegen een lokale productie-build: catalogus-select toont gegroepeerde items, een gekozen item blijft bewaard na opslaan, en de publieke producentpagina toont de "Past bij: Eieren"-regel voor het gekoppelde testproduct zonder dat de publicatiestatus van het aanbod erdoor verandert.

**Aanvulling 5 aug 2026, deel 43 (favorieten: opgeslagen producenten, huishouden-breed)**: vierde en laatste PR van spoor 1 in "Batch 18" (bewust uitgestelde audit-features), CRO #70. Nieuwe `saved_producers`-tabel (household_id + producer_id, `unique` samen, `saved_by_user_id` alleen ter info met `ON DELETE SET NULL`) is bewust huishouden-breed en dus een ander concept dan `swipeSignals` (die is expres per-gebruiker: "smaak is persoonlijk"). **Login/huishouden-ontwerp**: `registerAction` hangt elk account altijd aan een huishouden, maar `leaveHouseholdAction` heeft geen guard tegen het verlaten van je enige huishouden, dus een ingelogde gebruiker kan tijdelijk zonder huishouden zitten. Nieuwe `toggleSavedProducerAction` (`producent/actions.ts`) geeft daarom drie losse foutresultaten: `"login"`, `"geen-huishouden"` en (verdediging tegen een verdwenen slug) `"niet-gevonden"`. Nieuwe `SaveProducerButton` (`useOptimistic`, hartje-icoon met een `filled`-prop) naast Cheffs/deel-knop op de producentpagina: toont altijd eerst de gewone knop (ook anoniem), en swapt pas na een klik met `error:"login"` naar een inlog-link, zelfde volgorde-idee als `AskChefsButton`. Nieuwe sectie "Opgeslagen producenten" op `/profiel` na het huishouden-blok. Nieuw event `producent_opgeslagen` (alleen bij opslaan gelogd, niet bij verwijderen) inclusief dashboardlabel. Geverifieerd tegen een lokale productie-build: anonieme knop toont na een klik de inlog-link, ingelogd togglen werkt optimistisch en overleeft een reload, de producent verschijnt/verdwijnt correct op `/profiel`, en het event staat met de juiste `producerSlug`-property in de database.

**Aanvulling 5 aug 2026, deel 44 (homepage-onboarding: locatie-invoer)**: eerste PR van spoor 2 in "Batch 18" na de mechanische PR 2.1. De homepage had geen enkele locatie-ingang: een nieuwe bezoeker moest eerst een lijst starten en dan pas op de lijstpagina een postcode invullen voordat er lokale matches zichtbaar werden. Nieuwe `HomeLocationInput.tsx` spiegelt `LocationSearch.tsx` visueel (postcode-veld + "Gebruik mijn locatie") maar navigeert bewust niet naar `/producenten`: hij geeft de opgeloste `{lat, lng, label}` aan een callback, met twee nieuwe dunne server actions (`resolveLocationByQueryAction`/`resolveLocationByCoordsAction`, hergebruiken puur `geocode()`/`reverseGeocode()` uit `geocode.ts`) omdat er op de homepage nog geen lijst-token bestaat om de bestaande `setLocationByQueryAction` op te richten. `createListAction`/`createSampleListAction` kregen een **optioneel** derde `location`-argument (bestaande aanroepers ongewijzigd): bij een locatie wordt meteen na het aanmaken `setListLocation()` aangeroepen, geen aparte navigatiestap of dubbele geocode-call. `HomeListPanel` houdt de opgeloste locatie in lokale state en geeft 'm door aan beide create-handlers; een kruisje wist de keuze weer terug naar het invoerveld. Geen schema-wijziging. **Twee false alarms tijdens het verifiëren** (geen echte bugs, wel het vermelden waard): de eerste Playwright-run tegen de lokale productie-build zag de locatie niet op de nieuwe lijstpagina staan binnen 1-2s na de `router.push`; een rechtstreekse databasecheck bevestigde dat `lists.postcode`/`lat`/`lng` al meteen correct stonden, en een langere wacht (4-5s) liet de tekst alsnog verschijnen — een koude lokale `next start`-server is simpelweg trager dan een echte Vercel-deployment, geen productiebug (zelfde soort les als de hydratie-timing bij PR 1.1's favorieten-knop). Geverifieerd tegen een lokale productie-build: locatie-invoer zichtbaar op de homepage, postcode-zoekopdracht en "Gebruik mijn locatie" (met een gemockte Chromium-geolocation) lossen beide correct op, het kruisje wist de keuze, en zowel een gewone lijst als de voorbeeldlijst krijgen de gekozen locatie mee in de database.

Met deze PR is de volledige audit (spoor 0: 5 PR's scherpe randen, spoor A: 6 PR's boer-als-webshop, spoor B: 6 PR's lijst-hergebruik/plannen — 17 PR's in totaal) afgerond en live. Openstaande punten uit de audit die bewust niet in deze reeks zaten: een echt support-e-mailadres (zie Openstaand hierboven), web push (laag 3 van CRO #83), en de eerder genoemde "buiten dit plan"-items (provincie-SEO-pagina's per catalogusitem, homepage-onboarding, beheer-bulktools, registratie-event/retentie, aanbod-verjaring).

**Icoon-dekking geverifieerd (3 aug 2026)**: alle 147 catalogusitems hebben nu een eigen, uniek SVG-icoon (147 iconen, 0 gedeeld, 0 items op de categorie-fallback). Enige gevonden gat: "gehakt" hergebruikte het rundvlees-icoon (plak met beenmerg, feitelijk onjuist voor gehakt); gefixt met een nieuw icoon (korrelig hoopje). Ook een dode mapping opgeruimd (verwees naar een niet meer bestaand catalogusitem "vis").

**Aanvulling 3 aug 2026, deel 6 (interactie-bijstelling)**: tegels zijn weer een echte **Bring-toggle** (tik = op de lijst, nog een tik = er weer af; aantallen via vasthouden). Daarmee vervalt "tweede tik = +1" (eerdere suggestie van Chimene); verwijderen zat te verstopt achter vasthouden, en regel 1 (doe wat Bring doet) geeft de doorslag. Introkaart-tekst aangepast. Alle onderste panelen (aantal-paneel, drawer, zoekbalk, snackbar) eindigen nu gegarandeerd boven de tabbalk.

**Datakwaliteit-audit (3 aug 2026)**: volledige meting van de productiedatabase, zie [docs/DATAKWALITEIT.md](DATAKWALITEIT.md). Kernpunten: geo/product-dekking is sterk (99,6% coördinaten, 91% producten), maar er is maar **1 record ooit geverifieerd** (`last_verified_at`) en **alle 2.280 producenten stonden op `kind = "boerderijwinkel"`**. Twee snelle fixes doorgevoerd: openingstijden-parser herkent nu ook "Dagelijks (...)"-varianten (1693 → 1709 parseerbaar), en `kind` is afgeleid uit de bedrijfsnaam (`scripts/classify-kind.ts`, idempotent): 31 imkerij, 7 wijngaard, 3 brouwerij (bakkerij bewust overgeslagen, te onbetrouwbaar signaal via de veelvoorkomende achternaam "Bakker"). Naamduplicaten-check toegevoegd aan `data-quality.ts`. Nog open: 37 bekende adres-duplicaten (oplosbaar via `/beheer/producenten/duplicaten`, vereist een blik per geval) plus 13 naamduplicaten, en contactgegevens (telefoon 5%, website 13%) wachten op de Google Places-sync.

**Openstaand**:

- **GitHub Actions is geblokkeerd door een billing-probleem op het account van frank2889** ("account locked due to a billing issue"): de verplichte CI-check kan niet draaien, dus PR's van Chimene/Sally kunnen niet gemerged worden en de eigenaar moet steeds bypassen. Oplossen via github.com > Settings > Billing and plans (Actions is gratis voor publieke repo's, dus na herstel kost het niets).
- PR #1 (docs/SAMENWERKEN.md, de samenwerkgids voor niet-developers) staat bewust open als review-oefening voor Sally of Chimene; Sally moet ook nog haar repo-uitnodiging accepteren (GitHub: wissalakmadi).
- **Delen is nog minimaal**: "Deel deze lijst" doet alleen de OS-share-sheet of link kopiëren, er is nergens zichtbaar met wie je een lijst deelt of wie er al is toegevoegd. Frank opperde ook een in-app chat binnen de lijst: **chatten tussen de echte mensen op een gedeelde lijst** (nadrukkelijk géén AI-chat). Scope zoals Frank hem schetst: **per account en per boodschappenlijst**, zodat je elkaar dingen kunt vragen ("neem jij ook melk mee?"). Eerste stap is vermoedelijk een "wie doet mee"-overzicht (leunt op de bestaande household_members-data); de chat zelf is een apart project (berichten-tabel gescopet op lijst, ingelogde deelnemers, ongelezen-badges, realtime via Pusher, moderatie).
- **Echte push-notificaties/geofencing** (vlakbij-melding op de achtergrond): kan niet in een PWA (iOS geeft webapps geen background-location) en `public/sw.js` heeft geen push-listeners; hoort bij de Expo-app. Web-push met de app open zou kunnen als tussenstap (vergt SW-listeners + subscriptie-opslag). De vlakbij-melding v1 (deel 10) werkt alleen met de lijstpagina open.
- **Account-brede instellingen** voor koper (meldingsradius, voorkeuren) en verkoper: de users-tabel heeft nog geen instellingenkolom; de vlakbij-instelling is bewust device-lokaal (localStorage). Zodra er meer instellingen komen (notificaties, dieetvoorkeuren) een echte instellingen-sectie op /profiel en /portaal bouwen.

- **Pusher-keys aanmaken** (gratis account, 4 env-vars in Vercel) - realtime i.p.v. 10s-polling
- **Google Places-key aanmaken** (`GOOGLE_MAPS_API_KEY`) - vult in één keer openingstijden, telefoon, website en gestopt-status aan (`scripts/google-sync.ts --match` eenmalig, `--refresh` maandelijks); ~$40-80/maand bij volledige dekking, vereist "powered by Google"-vermelding
- **Week echte-gebruikerstest** (3-5 mensen incl. 65+) - belangrijkste volgende stap; ook de swipe-tab en tegel-toggle zelf op een telefoon voelen
- Definitieve naam + domein; daarna Search Console + SEO-quick-wins (canonicals, og:image, dubbele titel-suffix, interne links producent->provincie)
- Neon van us-east-1 naar EU-regio; voorwaarden via jurist
- Eerste 25-50 leden werven ("founding members"); de beheeromgeving `/beheer` staat hiervoor klaar (aanmeldingen goedkeuren, meldingen, producentenbeheer, cijfers)
- Zelf-claim-flow voor het portaal (verkoper koppelt zichzelf met mailverificatie; vereist eerst een besluit over transactionele mail). Portaal v1 met team-koppeling is live.
- **Duplicaten opruimen** (37 adres- + 13 naamduplicaten, via `/beheer/producenten/duplicaten`; vereist een blik per geval, zie DATAKWALITEIT.md)
- Fase 3: marktstandplaatsen, slimme dekking, sjablonen, recepten-import, aanbiedingen (incl. locatiegebonden), deboervinder-migratie met 301's; daarna Expo-app
- Offline-beperking v1: pagina herladen zonder verbinding verliest nog niet-gesyncte vinkjes

---


## Context

Gestart als herbouw van deboervinder.nl (kaart met 2.279 boerderijwinkels, live op onlyfarms-ten.vercel.app). Na 20 richtingvragen is het product wezenlijk gepivoteerd:

**De kern is een gedeelde boodschappenlijst naar het model van de app Bring!** — visuele producttegels, meerdere lijsten per huishouden, samen afvinken — gematcht aan **waar je elk item bij een lokale producent in de buurt koopt**. Geen kaart meer, nergens: navigeren doe je met je eigen telefoon (route-knop → Google/Apple Maps).

**Scope verbreed**: niet alleen boerderijwinkels maar **alle lokale producenten van eet- en drinkwaar met KVK-inschrijving** — brouwers, bakkers, imkers, wijngaarden, kaasmakers. Alcohol gewoon tonen met NIX18-vermelding.

### Alle besluiten uit de vragenronde (samengevat)

| Onderwerp | Besluit |
|---|---|
| Doelgroep v1 | Gezinnen/thuiskoks die bewust (deels) lokaal inkopen |
| deboervinder.nl | Wordt **vervangen** door dit platform (redirects + SEO-waarde overzetten bij livegang) |
| Naam | "OnlyFarms" is **werktitel** — naam-onafhankelijk bouwen (één config-constante voor naam/logo/meta) |
| Launch | Pas live als het áf voelt; tot die tijd besloten previews |
| Lijsten | **Meerdere lijsten** per huishouden (Bring-model), delen via geheime link, eerst zonder account |
| Realtime | **Echt realtime** afvinken (1–2 s) via Pusher Channels (gratis tier); polling als fallback |
| Item-details | Aantal/hoeveelheid + notitie in MVP; **foto's** zodra Vercel Blob erin zit (fase 2) |
| Suggesties | Seizoenssuggesties + "vorige keer gekocht" |
| Producenten-instroom | Alle drie: redactie verzamelt actief, zelf aanmelden (KVK-flow), gebruikers dragen aan |
| Gids vs. leden | **Gescheiden**: de 2.279 bestaande vermeldingen zijn een informatieve *gids*; alleen aangemelde *leden* draaien mee in de lijst-matching. Gids-vermeldingen tonen als "staat in de gids — nog niet aangesloten" met claim-knop (brug naar lidmaatschap) |
| Productdata (78% leeg) | Automatisch categorieën afleiden uit omschrijvingsteksten + steekproefcontrole |
| Zoekafstand | Standaard 10 km, instelbaar (5/15/25), en altijd fallback "5 dichtstbijzijnde" als de radius leeg is |
| Reviews | **Alleen tekst-ervaringen** — geen sterren (vriendelijk voor kleine producenten) |
| Moderatie | Zo min mogelijk handwerk: KVK-validatie via KVK Zoeken-API, woordfilters, alleen randgevallen handmatig |
| Notificaties | Via **Klaviyo** — het platform stuurt server-side events; Sally bouwt de flows, Chimene doet socials |
| Monetisatie | **Eerst gratis groeien**; betaalde opties pas bij bewezen verkeer (platform blijft betalingsvrij — Marktplaats-model) |
| Analytics | **Plausible** (cookieloos teamdashboard) + **GA4** (met consent-banner) |
| Taal | NL, maar **i18n-klaar** (alle teksten in taalbestanden vanaf dag één) |
| Succes na 6 mnd | Dashboard met doel per pijler: wekelijkse lijst-gebruikers (retentie), aangesloten leden, SEO-verkeer |
| Design | Max **3 kleuren** (terra/ink/cream) met tintenschema's; hele site illustratief/cartoonish; **geen emoji — alleen SVG**; **seizoens- en weersgebonden thema** |
| Bring-assets | Tegel-UX nabouwen mag (interactiepatroon); hun SVG's/code alleen na schriftelijke aanlevering/licentie door Bring zelf — tot die tijd eigen illustraties in huisstijl (passen toch beter) |
| Toekomstige app | Next.js blijft (Next ís React); App Store later via Expo/React Native op dezelfde backend — logica framework-onafhankelijk in `src/lib/`, mutaties als dunne wrappers zodat er later een `/api/v1` naast kan |

---

## Bring-pariteit — wat, wanneer

| Bring-functie | Bij ons | Fase |
| --- | --- | --- |
| Meerdere lijsten, delen, samen afvinken | Idem, deel-link + realtime (Pusher) | 1 |
| Tegel-catalogus per categorie + zoeken | Idem, eigen SVG-tegels in huisstijl, zoekveld | 1 |
| Specificaties (aantal, notitie) | Idem | 1 |
| Afgevinkt → "onlangs gekocht" + snel opnieuw toevoegen | Idem, sectie onderaan per lijst | 1 |
| Seizoens-/aanbevolen sectie | Seizoenscatalogus bovenaan (sluit aan op seizoensdesign) | 1 |
| Pushberichten ("X toegevoegd") | Web-push (PWA) + Klaviyo-mails | 2 |
| Profiel (naam/foto) | Idem, bij accounts | 2 |
| Foto bij item | Vercel Blob | 2 |
| Lijstsjablonen ("standaard week") | Idem | 3 |
| Recepten-import (URL → items) | Idem, mooi groeikanaal | 3 |
| "Angebote" (aanbiedingen/folders) | **Aanbiedingen van leden** — tevens de monetisatie-parallel | 3/4 |
| Klantenkaarten/wallet, supermarktfolders | **Bewust niet** — past niet bij lokaal | — |

## Locatie-UX ("vanaf mijn locatie")

- Primair: **"Gebruik mijn locatie"-knop** (browser-geolocation) — coördinaten direct bruikbaar voor afstandsberekening, geen geocoding nodig.
- Fallback/correctie: postcode of plaats via PDOK.
- Locatie wordt onthouden per apparaat (localStorage) én per lijst (voor gedeelde matching); overal een klein "wijzig locatie"-affordance.
- Afstand zichtbaar op elke producentkaart; radius instelbaar (10 km default, 5/15/25), fallback "5 dichtstbijzijnde" bij lege radius.
- "Onderweg"-modus (locatie live verversen + reminder nabij producent, zoals Bring's winkelherinnering): fase 3.

## Datastrategie — bestaand slim gebruiken + betere bronnen

**Bestaande data (2.279 records) maximaal benutten:**

1. `derive-products.ts`: categorieën afleiden uit omschrijvingen (78% leeg → naar verwachting grotendeels gevuld) + steekproefcontrole.
2. De 5 records met (0,0)-coördinaten alsnog geocoderen via PDOK (adres is aanwezig).
3. De 37 dubbele adressen deduperen (script + handmatige check).
4. 3 Belgische records: besluit (verwijderen of "grensstreek"-vlag).
5. **Volledigheidsscore** per producent (heeft producten/tijden/site/telefoon?) → werklijst voor de redactie, en "laatst bevestigd"-transparantie op de site.

**Nieuwe bronnen (redactie-leads, gefaseerd):**

- **Skal-register** (publiek register bio-certificering): bio-claims verifiëren én lead-lijst van gecertificeerde producenten — maakt onze "bio"-badge hard i.p.v. zelfverklaard.
- **KVK Zoeken-API op SBI-codes** (bakkerijen, brouwerijen, zuivel, vleesverwerking, tuinbouw): lead-lijsten per branche voor de gids-uitbreiding naar alle producenten.
- OSM / VVV- en gemeentelijsten / streekkeurmerken: alleen als research-leads voor de redactie (OSM-data niet 1-op-1 in de productie-db i.v.m. ODbL share-alike).
- Gebruikers ("mis je een producent?") en leden-aanmeldingen als doorlopende instroom.

## Ontwerpsysteem

**Tokens** (Tailwind v4 `@theme` in `src/app/globals.css`, enige plek met hexwaarden):
- `terra` 50–900 (hoofdkleur, terracotta; 500 = #C4552C), `ink` 300–900 (donker neutraal), `cream` 50–300 (achtergrond). Dark mode schrappen (bewust licht); alle bestaande `green-*`/`neutral-*`/`dark:` classes vervangen.

**Seizoens- en weerthema**:
- `src/lib/season.ts`: bepaalt seizoen (datum) → thema-object met accent-tint (binnen de 3 families: lente = terra-300-accenten, zomer = terra-400, herfst = terra-600, winter = ink-zwaarder), illustratie-set en seizoenscatalogus-items.
- Illustratieve headers/lege-staten per seizoen (eigen SVG's, uitbreiding van `src/components/icons.tsx` naar `src/components/illustrations/`).
- Weer (subtiel, fase 2+): Open-Meteo (gratis, geen key) voor een klein accent ("mooi fietsweer om de kaasboer te bezoeken") — nooit blokkerend.

**Naam-onafhankelijk**: `src/lib/brand.ts` met `BRAND = { name, tagline, domain }` — overal uit putten, nergens hardcoden.

**i18n-klaar**: alle UI-teksten in `src/messages/nl.json` (next-intl of lichte eigen helper); componenten bevatten geen letterlijke strings meer.

---

## Datamodel (wijzigingen op bestaand schema in `src/db/schema.ts`)

- `farms` → hernoemen naar **`producers`** + nieuwe velden: `kind` enum (`boerderijwinkel | brouwerij | bakkerij | imkerij | wijngaard | overig`), `isMember` boolean (default false — de gids/leden-scheiding), `claimedBySellerId` FK naar bestaand `sellers`. Bestaande 2.279 records = gids (`isMember: false`).
- Bestaand `sellers`/`offers` blijft de leden-kant; `offers.category` koppelen aan catalog-keys (voedt de matching).
- Nieuw: `lists` (id, token, naam, postcode+lat/lng, seizoen-onafhankelijk), `list_items` (list FK, catalog_key nullable, label, qty, note, checked, position), later fase 2: `users` (Auth.js), `list_members`, `visits`, `experiences` (tekst-ervaringen met moderatiestatus), `wishlist`.
- `farm_reviews` uit eerder plan vervalt → `experiences` (alleen tekst, geen rating-kolom).

**Datavul-script** (`scripts/derive-products.ts`): leidt catalog-keys af uit `description`-teksten (keyword-matching per categorie), schrijft met `source: 'derived'`-markering; rapport + steekproeflijst voor controle.

---

## Fasering (elke stap deploybaar; launch pas als fase 1+2 áf voelen)

### Fase 0 — Fundament
1. `brand.ts`, kleurtokens, dark mode eruit, i18n-opzet, seizoensthema-mechanisme.
2. Kaart eruit: `FarmMap.tsx`, `KaartClient.tsx`, leaflet-deps weg; `/kaart` → redirect `/producenten`.
3. Schema-migratie farms→producers + kind/isMember; datavul-script draaien + steekproef.
4. Plausible + GA4 (consent-banner alleen voor GA4-cookies) + simpel doelen-dashboard (kan later).

### Fase 1 — Kern: lijst + matching + gids (anoniem)
1. **Catalogus** `src/lib/catalog.ts`: items per categorie (incl. brood, bier, wijn, honing...; alcohol-items dragen `nix18: true` → vermelding in UI), elk met eigen SVG-tegel in huisstijl, seizoensmarkering per item.
2. **Lijsten**: meerdere lijsten, tegel-grid + zoekveld om toe te voegen (Bring-UX), qty/notitie, afvinken → "onlangs gekocht"-sectie met snel opnieuw toevoegen, delen via `/lijst/[token]`; realtime via Pusher Channels (`lijst-{token}`-kanaal, events bij elke mutatie), polling-fallback. Mutaties via server actions die `src/lib/queries/lists.ts` aanroepen.
3. **Matching per item**: locatie via "Gebruik mijn locatie"-knop (geolocation) of postcode (PDOK, `src/lib/geocode.ts`), onthouden per apparaat + per lijst, overal wijzigbaar; per item: leden die het voeren binnen radius (10 km default, instelbaar 5/15/25, fallback 5 dichtstbijzijnde — Haversine in SQL), afstand op elke kaart; daaronder gids-vermeldingen "nog niet aangesloten" met route-knop en claim-teaser.
4. **SEO-pagina's**: `/producent/[slug]` (server-rendered, JSON-LD LocalBusiness, route-knop, meldknop → `reports`, claim-knop), `/producenten` (zoeken op afstand + categoriefilters, hergebruik filterchips-UI), `/provincie/[slug]`, `sitemap.ts`, `robots.ts`.
5. **Homepage**: lijst-propositie, seizoensheader, categorie-grid.
6. **Klaviyo-events** (server-side API): `list_created`, `item_added`, `producer_claim_started` — Sally bouwt er flows op.

### Fase 2 — Accounts, profielen, leden
1. Auth.js (magic link + Google); anonieme lijsten claimen; `list_members` (huishouden).
2. Profiel: bezochte producenten (check-in), tekst-ervaringen (moderatiewachtrij met woordfilter-voorcheck), wishlist; foto's bij items/ervaringen via Vercel Blob.
3. Leden-flow afmaken: aanmelden (bestaand) + **KVK Zoeken-API-validatie** (automatische check van nummer/naam), aanbod beheren gekoppeld aan catalogus, "geverifieerd lid"-badge; gids-vermelding claimen = gids-record wordt lid.
4. Admin `/beheer`: GEDAAN (aug 2026). Rol-gebaseerd (`users.role` = "team", check per request via `requireAdminUser`), dashboard met kerncijfers en groei, wachtrijen voor meldingen (met afhandelnotitie), verkopersaanmeldingen (goedkeuren = koppelen aan gids-record of nieuw record aanmaken; Klaviyo-events `seller_approved`/`seller_rejected`/`seller_suspended`), ervaringen-moderatie, producentenbeheer (bewerken zet `last_verified_at`) en duplicaten-wachtrij. Vervolg: producentenportaal `/portaal` (fundament ligt er: `claimed_by_seller_id`/`claimed_by_email` worden bij goedkeuring gezet, het bewerkformulier kan met een veld-subset hergebruikt worden; blocker is transactionele mail voor claim-verificatie).

### Fase 3 — Slim & groei
1. Slimme dekking: "dit lid dekt 5 van je 7 items" (set-cover over leden-matches).
2. Suggesties: seizoen + eerder gekocht; seizoensnotificatie-segmenten naar Klaviyo.
3. deboervinder.nl-migratie: 301-redirects per boerderij naar `/producent/[slug]`, domein/merk-switch (naam dan definitief).
4. Monetisatie-verkenning pas hierna (gratis groeien): uitgelicht-vermeldingen, premium ledenprofiel — betalingsvrij platform blijft uitgangspunt.

---

## Belangrijkste bestanden

- **Weg**: `src/components/FarmMap.tsx`, `KaartClient.tsx`, leaflet-dependencies.
- **Nieuw**: `src/lib/{brand,season,catalog,geocode,realtime,klaviyo}.ts`, `src/lib/queries/{lists,producers}.ts`, `src/messages/nl.json`, `src/components/illustrations/`, `src/app/lijst/**`, `src/app/producent(en)/**`, `src/app/provincie/[slug]/`, `src/app/sitemap.ts`, `src/app/robots.ts`, `scripts/derive-products.ts`.
- **Aangepast**: `src/db/schema.ts` (producers, lists, list_items; fase 2: users/visits/experiences/wishlist), `globals.css` (tokens), `page.tsx` (homepage), `verkopen` (KVK-validatie, teksten uit taalbestand), README (nieuwe architectuur + rollen: Frank dev, Chimene socials, Sally Klaviyo-automations).

## Verificatie
1. `npm run lint && build`; migraties via `db:generate`/`db:push`; datavul-script met rapport + handmatige steekproef van 25 records.
2. Flow-test op preview: 2 telefoons, zelfde lijst — item toevoegen/afvinken verschijnt binnen 2 s bij de ander (Pusher) en na refresh (fallback).
3. Matching: postcode in dunbevolkt gebied → fallback "5 dichtstbijzijnde" werkt; alcohol-item toont NIX18-vermelding.
4. SEO: `curl` producent-pagina toont content + JSON-LD; sitemap geldig; oude `/kaart` en boerderij-URL's redirecten.
5. Design-checks: grep op `green-|neutral-|dark:` leeg; geen emoji; seizoenswissel testen door datum te mocken.
6. Klaviyo: testevent zichtbaar in Klaviyo-dashboard (Sally bevestigt).

## Openstaand (geen blokkade, wel agenderen)
- Definitieve naam + domein (nodig vóór publieke launch en deboervinder-migratie).
- Schriftelijke Bring-toestemming als hun assets echt gewenst zijn.
- Gebruikersvoorwaarden door jurist (platformrol, NIX18, UGC/ervaringen).
- Neon-database verhuizen van us-east-1 naar EU-regio (latency + AVG-netheid) — 10 min werk, vóór launch doen.
- **Echt support-e-mailadres voor `BRAND.supportEmail`**: vier plekken in het portaal ("Neem contact met ons op") verwijzen nergens naartoe (geen enkel e-mailadres komt voor in de codebase, dus bewust niet verzonnen). Zodra dit adres er is: `src/lib/brand.ts` + mailto-links onder `portal.noSellerText`/`rejected`/`suspended`/`noListing`.
