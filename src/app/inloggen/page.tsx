import InloggenClient from "@/components/InloggenClient";

export const metadata = {
  title: "Inloggen",
  robots: { index: false, follow: true },
};

export default function InloggenPage() {
  return <InloggenClient />;
}
